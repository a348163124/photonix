# Screenshots

Drop UI captures here, named to match the references in the root README:

- `library.png` — the Library grid with thumbnails
- `editor.png` — Editor with mask + prompt panel visible
- `generate.png` — Generate tab with a result and gallery
- `settings.png` — Settings (Provider or Editing tab)

Recommended:

- 1280×800 or larger
- PNG (preferred) or JPEG
- Keep file size under ~500KB if possible (use `Recommended` export profile or external tool)
- Crop or blur any real API keys / personal paths before committing
